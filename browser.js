/**
 *  Browser host. Simple for now. This is where we serve everything.
 *  The browser version.
 */

function host() {
  return '';
}

host.port = null;
host.protocol = 'http';
host.name = '';

module.exports = host;
